<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DocRedactorApp.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DocRedactorApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <!-- Toast-style InfoBar at the top, hidden by default -->
        <InfoBar
            x:Name="UnsupportedFileBar"
            VerticalAlignment="Top"
            Margin="24,24,24,0"
            IsOpen="False"
            Severity="Warning"
            Title="Unsupported file"
            Message="DocRedactor only accepts .xps and .oxps files."
            Canvas.ZIndex="10"/>

        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="24"
            MaxWidth="480">

            <FontIcon
                Glyph="&#xE72E;"
                FontSize="80"
                HorizontalAlignment="Center"
                Foreground="{ThemeResource SystemAccentColorLight2}"/>

            <TextBlock
                Text="DocRedactor"
                Style="{ThemeResource TitleLargeTextBlockStyle}"
                HorizontalAlignment="Center"/>

            <TextBlock
                Text="Detect and redact PII from XPS documents"
                Style="{ThemeResource BodyTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                HorizontalAlignment="Center"
                TextAlignment="Center"
                TextWrapping="Wrap"/>

            <Border
                x:Name="DropZone"
                AllowDrop="True"
                DragOver="DropZone_DragOver"
                DragLeave="DropZone_DragLeave"
                Drop="DropZone_Drop"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                BorderThickness="2"
                CornerRadius="8"
                Background="{ThemeResource ControlFillColorDefaultBrush}"
                Padding="32"
                MinWidth="400"
                MinHeight="160"
                Margin="0,16,0,0">

                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="12">
                    <FontIcon
                        Glyph="&#xE8E5;"
                        FontSize="40"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock
                        Text="Drop an XPS or OXPS file here"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <TextBlock
                Text="or"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                HorizontalAlignment="Center"/>

            <Button
                x:Name="OpenFileButton"
                Content="Open File..."
                Style="{ThemeResource AccentButtonStyle}"
                Click="OpenFileButton_Click"
                HorizontalAlignment="Center"
                MinWidth="160"/>
        </StackPanel>
    </Grid>
</Page>